stages:
  - build

build:
  image: itau-docker-base.depdes.artifactory.prod.cloud.ihf/conan:$CONAN_VERSION
  stage: build
  allow_failure: false
  script:
    - make
  artifacts:
    expire_in: 1 day
    paths:
      - build/
  tags:
    - ci
  except:
    variables:
      - $DISABLE_BUILD
