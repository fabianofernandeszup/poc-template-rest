tag_helper:
    only:
        - master

build:
    only:
        - branches
        - tags

test:
    only:
        - branches
        - tags

inspection:
    only:
        - /^feature/.*$/
        - master
        - tags

package:
    only:
        - /^feature/.*$/
        - tags

publish:
    only:
        - /^feature/.*$/
        - tags

deploy_dev:
    only:
        - /^feature/.*$/
        - tags
        
deploy_hom:
    only:
        - tags
    except:
        - branches

governance:
    only:
        - tags
    except:
        - branches

deploy_prod:
    only:
        - tags
    except:
        - branches
