include:
  - local: /ci/java_maven.yaml
  - local: /ci/package/maven.yaml
  - local: /ci/package/rpm.yaml
  - local: /ci/publish/artifactory.yaml
  - local: /cd/deploy_puppet.yaml

stages:
  - build                 # jobs: build (branches e tags)
  - test                  # jobs: unit_test (branches e tags)
  - inspection            # jobs: docker_best_practice (branches), quality_check (branches), quality (tags), security_check (branches), security (tags)
  - package               # jobs: package (branches e tags), package_rpm (branches e tags)
  - publish               # jobs: publish (tags)
  - deploy_dev            # jobs: deploy_dev (master)
  - deploy_hom            # jobs: deploy_hom (tags)
  - governance            # jobs: create_jira_ticket (tags)
  - deploy_prd            # jobs: deploy_prd (tags)