include:
  - local: /ci/build/angular.yaml
  - local: /ci/test/angular.yaml
  - local: /ci/inspection/quality.yaml
  - local: /ci/inspection/security.yaml  

quality:
  cache:
    key: build
    paths:
      - $ANGULAR_PROJECT_PATH/node_modules/
    policy: pull

quality_check:
  cache:
    key: build
    paths:
      - $ANGULAR_PROJECT_PATH/node_modules/
    policy: pull
